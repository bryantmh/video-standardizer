# Double-click launcher for Video Standardizer GUI.
# The .pyw extension makes Windows run this with pythonw.exe,
# which opens the GUI without spawning a console window.
from video_standardizer import launch_gui
launch_gui()
